# 本地跨域配置（前端、Swagger 都要放行）
CORS_ORIGINS=http://localhost:3000,http://localhost:9000,http://127.0.0.1:9000

# 数据库配置（直连本机 MySQL 容器，端口 3307）
MYSQL_DSN=root:pass@tcp(127.0.0.1:3307)/qingverse?parseTime=true&charset=utf8mb4

# API 监听地址（本地开发）
ADDR=127.0.0.1:9000

# 管理员 Token（保护 /api/contact GET）
ADMIN_TOKEN=dev-token-or-empty

# Markdown 内容目录（相对 api 目录）
CONTENT_DIR=../../content