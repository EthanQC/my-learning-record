# CI/CD 镜像仓库（GitHub Actions 会覆盖 API_TAG / WEB_TAG）
REGISTRY=your-registry.example.com/namespace
API_TAG=
WEB_TAG=

# 域名 & CORS（生产域名；多个用逗号）
DOMAIN=yourdomain.com, www.yourdomain.com
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# Web 构建/运行时用于前端请求 API 的公网地址
PUBLIC_API_BASE_URL=https://yourdomain.com

# MySQL（容器内部使用 mysql:3306，不暴露端口）
MYSQL_ROOT_PASSWORD=root_password_here
MYSQL_USER=app
MYSQL_PASSWORD=app_pass
MYSQL_DATABASE=qingverse

# API
ADMIN_TOKEN=your-admin-token-for-protected-endpoints
